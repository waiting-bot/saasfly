{
  "buildCommand": "cd ../.. && turbo run build --filter nextjs",
  "installCommand": "bun install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "devCommand": "turbo run dev --parallel",
  "rootDirectory": "apps/nextjs",
  "regions": ["hkg1"],
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  }
}
