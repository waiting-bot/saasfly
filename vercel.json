{
  "buildCommand": "cd apps/nextjs && bun run build",
  "installCommand": "cd apps/nextjs && bun install",
  "outputDirectory": "apps/nextjs/.next",
  "framework": "nextjs",
  "devCommand": "turbo run dev --parallel",
  "regions": ["hkg1"],
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  }
}
